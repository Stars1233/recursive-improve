# Required: Anthropic API key for the recursive reflector (Sonnet + Haiku)
ANTHROPIC_API_KEY=sk-ant-...

# Optional: OpenAI API key for dedup embeddings (only needed with --dedup flag)
# OPENAI_API_KEY=sk-...
